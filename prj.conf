CONFIG_SPI=y
CONFIG_SPI_NRFX=y # enables Nordic-specific SPI driver
CONFIG_SPI_ASYNC=y # enables asyn (non-blocking) SPI API in Zephyr
CONFIG_GPIO=y

CONFIG_LOG=y        # enables logging errors
CONFIG_CONSOLE=y    # enables use of k_printk() and others; might be useful for debugging

